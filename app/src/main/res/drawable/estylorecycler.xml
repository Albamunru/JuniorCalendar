<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <padding
        android:bottom="10dp"
        android:left="10dp"
        android:right="10dp"
        android:top="10dp" />

    <!-- Ajusta el radio de las esquinas para hacer los bordes más redondeados -->
    <corners android:radius="30dp" />

    <!-- Cambia el color de fondo para que resalte más -->
    <solid android:color="#80FFFFFF" />


</shape>